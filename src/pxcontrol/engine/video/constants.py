"""Общие константы конвейера обработки (порт из makeVideo)."""

# Вписывание кадра в рамку 1920x1080 с сохранением пропорций, без чёрных полей.
# force_original_aspect_ratio=decrease — масштабируем так, чтобы кадр целиком влез
# в рамку (ограничивающую сторону выбирает ориентация кадра).
# force_divisible_by=2 — округляем стороны до чётных (требование кодека H.264).
FULLHD_FIT = "scale=1920:1080:force_original_aspect_ratio=decrease:force_divisible_by=2"

# Формат пикселей для совместимости с большинством плееров и H.264.
TARGET_PIX_FMT = "yuv420p"
