# Дорожная карта

> Статус: 🟢 актуально · Обновлено: 2026-09-20 · Владелец: olegosx

## Назначение документа

Показать порядок развития проекта: что делаем сначала, что потом. Помогает
не браться за всё сразу и собрать работающий минимум (MVP) раньше.

## Этап 0. Планирование — ✅ завершён

- [x] Создан каркас документации.
- [x] Зафиксированы решения: Bot API (ADR-0001), десктоп + PySide6 (ADR-0006), стек.
- [x] Наполнен `vision.md`; выбран стек (`tech-stack.md`).
- [x] Модель данных наполнена (`data-model.md`, 🟢); `use-cases.md` —
      сценарии UC-1/UC-2 реализованы, остальные ждут своих этапов.

## Этап 1. MVP — видео-пост вручную — ✅ завершён

Публикация работает целиком и проверена живьём; открытым остался
один необязательный пункт — встроенный просмотр видео.

- [x] Подключение канала: через userbot или бота, проверка прав,
      управление доступами (2026-07-12, доступы — 2026-07-14).
- [x] Подготовка видео (порт `makeVideo`): FullHD, вотермарк, заставка,
      обложка; пресеты; страница «Видео» (2026-07-12).
- [x] Публикация всех типов контента (`PostDraft`): userbot в приоритете,
      бот — запасной путь; «сейчас» и отложенно — сервер Telegram
      (ADR-0010, ADR-0011, 2026-07-14). Шаблоны подписей и имён файлов.
- [x] Пресет обработки по умолчанию для канала: настройка в карточке
      канала, подстановка на странице «Видео» (ADR-0013, 2026-07-16).
- [x] Пакетная обработка видео: рекурсивное сканирование папки, выбор
      файлов галочками, очередь обработки с прогрессом и отменой,
      подпапка пакета в результатах (ADR-0014, 2026-08-15).
- [x] Пакетная отправка: черновики постов из готовой папки, подписи
      по общему шаблону, раскладка времени стратегиями, правка
      построчно, атомарная постановка в очередь (ADR-0015, 2026-08-15).
- [x] Очередь сверх лимита отложек Telegram (100/канал): персистентная
      очередь отправки, ожидание слота, папка очереди на диске
      (ADR-0016, 2026-08-26).
- [x] Полный просмотр очереди отправки: кнопка «Вся очередь…»
      на «Публикации» — живой список со всеми элементами, сортировка
      (ближайшие/постановка/по каналам) и фильтры (статус, канал);
      на странице — ближайшие 20 карточек и сводка (2026-08-26;
      с 2026-09-17 — экран «Очередь» раздела «Публикация»).
- [x] Отложенные записи как очередь: правка текста и времени,
      «Сейчас» и «Удалить» на сервере Telegram; «Отложено» и «Очередь» —
      два списка одного вида; общий список карточек
      (ADR-0028, 2026-09-15; место в навигации — ADR-0032).
- [x] Обслуживание сообществ: чистка служебных записей и удаление
      мёртвых аккаунтов; под ними — общий каркас заданий движка
      и дорожка аккаунта Telegram (ADR-0024/0025/0026, 2026-09-12).
- [x] Кнопки под постом: публикует публикатор, разметку дорисовывает
      бот; у отложенных — выбор режима «публикация/кнопки важнее»;
      пределы клавиатуры проверяет приложение (ADR-0031, 2026-09-16;
      маршруты — [publishing-routes.md](../03-modules/publishing-routes.md)).
      Не сделано намеренно: склад (Р3) и отложенная пересылка (Р5) —
      нужны только кнопкам у крупных файлов в режиме «кнопки важнее».
- [x] Раздел «Публикация» по стадиям жизни поста (ADR-0032, подачи
      A1–A4 — все выполнены):
      - [x] **A1** — раздел с веткой подменю, экраны «Новый пост ·
            Очередь · Отложено», раздел «Расписание» снят, владение
            очередью отправки вынесено из формы поста в наблюдателя
            при главном окне (2026-09-17).
      - [x] **A2** — «Пакет» отдельным экраном: свой адресат (сообщество
            и тема), кнопки под постом на весь пакет, общий блок
            адресата с формой поста (2026-09-17).
      - [x] **A3** — экран «Опубликовано»: чтение ленты страницами
            публикатором, карточки с состоянием обещанных кнопок,
            переход на пост в Telegram (2026-09-17).
      - [x] **A4** — действия на «Опубликовано»: правка текста
            публикателем, кнопки ботом (в канале), удаление поста
            (2026-09-17). Замена вложения не сделана намеренно:
            загрузка файла — задание очереди, а не правка формы.
- [x] Хвосты ADR-0031: неудавшиеся обещания кнопок видны на карточке
      поста («Опубликовано», 2026-09-17); срок жизни обещания — сутки
      вместо пяти попыток по минуте (2026-09-17).
- [x] Проба кнопок в теме форума — последний хвост ADR-0031 закрыт
      (2026-09-17): бот отправляет в тему с клавиатурой, пост читается
      как запись темы. Форумом Telegram делает только группу, не
      связанную с каналом, — для проб заведена отдельная.
- [x] Стилизация текста поста (ADR-0033, подачи C1–C5 — все выполнены):
      - [x] **C1** — тип `RichText` и сущности, переводы в оба
            транспорта, хранение разметки в очереди (миграция
            `c4a9e17b6d35`), чтение разметки при правке на всех
            стадиях (2026-09-17).
      - [x] **C2** — поле «выделил — нажал стиль» во всех формах поста
            (новый пост, строки пакета, правка очереди, отложки
            и вышедшего поста); шаблоны подписей отдают размеченный
            текст; счётчик считает видимый (2026-09-17).
      - [x] **C3** — превью ссылки: выключить, крупное, над текстом
            (2026-09-17; крупное и «над текстом» — сырым запросом
            MTProto, живой пробы ещё не было).
      - [x] **C4** — альбом: пост стал списком файлов — движок,
            правила Telegram (`album_blocker`), обе формы поста
            и одна карточка на альбом в ленте «Опубликовано»
            (2026-09-17).
      - [x] **C5** — опрос: третий вид содержимого поста, оба
            транспорта, викторина, правка до отправки (2026-09-17).
            ADR-0033 выполнен целиком **и проверен живьём**
            (`_misc/tg_live_probe.py`): разметка, превью, альбом,
            опрос и викторина, кнопки, отложки, лента.
- [x] Учёт активности исполнителей чинён (2026-09-18): записи операций
      терялись после первой же выемки буфера, и страница аккаунта
      показывала ноль у круглосуточно работающего бота. Журнал операций
      стал объектом со своим правилом (ADR-0030).
- [x] Разбор очереди глазами (2026-09-18…19): состояние элемента
      показывает светофор вместо подписи мелким шрифтом, цвет метки
      слота выводится из времени (совпадений больше нет), у
      отправляющегося поста убрана нерабочая кнопка просмотра файла.
- [x] Все исполнители сообщества — на одной вкладке «Участники»
      (2026-09-18): пул userbot-аккаунтов и бот, там же назначение
      и отвязка; из «Настроек» строки про публикаторов убраны.
- [x] Аудит быстродействия и памяти (2026-09-19…20), шесть шагов
      по одному коммиту: одна панель параметров на страницу «Видео»
      вместо формы у каждого файла (190 файлов: 1,6 ГБ → 193 МБ);
      «Готовые видео» точечно страницами по 50; очереди по подписке
      вместо опроса — один наблюдатель на очередь при главном окне
      (ADR-0034, в покое ноль обращений к движку вместо шести
      в секунду); карточки списков обновляют части по своим отпечаткам,
      аватары из кэша; окна активности считает база агрегатами
      (96 мс → 34 мс на 15 тыс. строк); дашборды по отпечатку
      и очередь из кэша наблюдателя. Утечек не найдено: память
      определялась числом живых виджетов (стиль QFluentWidgets ставится
      каждому виджету отдельно).
- [ ] Длинные однородные списки на модели и представлении (`ListView`
      с делегатом строки) и пул карточек в `CardList` — решение
      за дизайнером: строка «Готовых видео» стоит ~1,3 МБ виджетами
      против ~70 КБ строкой модели (замер 19.09.2026).
- [x] Пул исполнителей сообщества (ADR-0035, этапы A–E, 2026-09-20):
      несколько пользователей и ботов в сообществе, полный снимок прав
      у каждого, назначения отдельно от прав, ввод исполнителя лестницей
      (вступление · ссылка-приглашение · приглашение силами пула),
      подбор исполнителя под действие. Проверено живьём 20.09.2026
      (проба `_misc/tg_join_probe.py` и настоящие операции): ввод
      в приватную группу по существующей ссылке, участник канала
      с реакциями и без права публиковать, ссылка-приглашение
      администратору. Дальше на этом фундаменте — распределение
      нагрузки публикации между исполнителями.
- [x] Диспетчер исполнителей и приоритет по срочности (ADR-0036):
      этап A — срочность и лестница приоритетов дорожки (2026-09-21) ✅;
      этап B — требования поста, диспетчер, публикатор из пула
      (2026-09-21) ✅; этап C — чтения, кнопки, боты, статистика,
      обслуживание и ввод через диспетчер, слоты группы всеми
      читателями (2026-09-21) ✅; этап D — лицо публикации: в группе
      от имени группы через явный `send_as`, ряд «От имени»
      (2026-09-21) ✅; этап E — очередь отправки параллельно между
      исполнителями, замки и удержания каркаса, потолок в три загрузки
      (2026-09-21) ✅. Живые проверки выполнены 21.09.2026
      (`_misc/tg_send_as_probe.py`): потолок поднят до 3, анонимность
      владельца читается по факту, анонимный бот пишет от себя.
- [ ] Встроенный просмотр видео (QtMultimedia); пока — системный плеер.

## Этап 2. Источники и очередь модерации

- [ ] Подключение источников (начать с RSS как самого простого).
- [ ] Очередь модерации с одобрением.

## Этап 3. Генерация ИИ

- [ ] Генерация текста по теме/стилю канала.
- [ ] Рерайт материалов из источников.

## Этап 4. Зрелость

- [ ] Аналитика, повторяющиеся расписания, роли пользователей.
- [ ] Наблюдаемость и оповещения об ошибках.

> Этапы и приоритеты — предварительные, уточняются по мере проработки
> [видения](../01-overview/vision.md).

## Связанные документы

- [Видение и цели](../01-overview/vision.md) · [Сценарии использования](../01-overview/use-cases.md)
