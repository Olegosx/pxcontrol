# Модуль «Подписи к постам»

> Статус: 🟢 реализовано · Обновлено: 2026-09-10 · Владелец: olegosx

## Назначение

Быстрая сборка подписей вида «жирное название + строки "Поле: #значения"»
на странице «Публикация». Цель — подпись к видео за несколько кликов.

## Модель

- **Поле канала** (`caption_fields`) — имя («Genre», «Year»…), оформление
  значений (решётки/текст), множественность, флаг «имя в подписи»
  (`show_name`: выключен — строка собирается без префикса «Имя: »,
  в подпись уходят только значения; переключается у существующего поля
  без пересоздания). У поля — **словарь значений**
  (`caption_values`), общий для всех шаблонов канала: значение, добавленное
  при сборке по одному шаблону, сразу видно в остальных.
- **Связь полей** (`parent_field_id`) — поле может жить внутри другого:
  «Character» внутри «Title». Тогда у значения словаря есть родитель
  (`parent_value_id`): персонаж принадлежит своему тайтлу.
- **Шаблон** (`caption_templates` + `caption_template_fields`) — именованный
  упорядоченный набор полей канала. Шаблонов у канала сколько угодно;
  в диалоге предвыбирается последний использованный (`last_used_at`).

## Поведение

- `CaptionsService` — CRUD полей/шаблонов, `record_usage` (автопополнение
  словарей использованными значениями, без дублей по регистру),
  `add_values`/`delete_value` — ручная правка словаря из редактора.
- Чистые функции: `build_caption` (название `**жирным**` — Markdown,
  Telethon применяет его по умолчанию; на запасном бот-пути транспорт
  сам переводит разметку в HTML для Bot API — см.
  [telegram-gateway.md](telegram-gateway.md); пустые поля пропускаются),
  `hashtag` («Tomb Raider» → `#TombRaider`), `title_from_filename`
  (имя файла без суффикса конвейера `_<пресет>_<штамп>`).
- Связанные словари: `set_field_parent` (объявить поле зависимым; смена
  связи обнуляет привязки значений — они указывали в прежний словарь),
  `assign_value_parent` (привязать значение руками),
  `add_values(..., parent_value_id)`, `FieldDto.available(parent_ids)` —
  значения, подходящие выбранному родителю.
- Интерфейс: на «Публикации» — «Собрать подпись…» (диалог: шаблон,
  название из имени файла, поля со словарями; ненужные поля отключаются
  галочками) и «Поля подписи…» (настройка пула полей и шаблонов;
  порядок полей шаблона — перетаскиванием; у каждого поля — кнопка
  «Словарь…»). Результат вставляется в поле подписи и правится руками.
  Оба — рабочие окна
  ([ADR-0023](../02-architecture/decisions/0023-work-windows-and-question-dialogs.md)):
  двигаются, меняют размер, длинные списки прокручиваются.
- **Редактор словаря** показывает значения пилюлями в поточной
  раскладке — теми же, что и при выборе значений в сборке подписи,
  только с крестиком удаления. Новые значения добавляются пачкой через
  запятую. У зависимого поля значения сгруппированы по родителю
  (заголовок — тайтл, в конце группа «Без привязки»); перенести
  значение в другую группу — правый щелчок по пилюле.

## Связанные словари: персонаж внутри тайтла

Поле объявляется зависимым от другого поля канала — в диалоге «Поля
подписи…» у поля есть выбор «внутри: …». После этого:

- **при сборке поста** список значений зависимого поля показывает только
  значения выбранного родителя: выбрали тайтл — видите его персонажей.
  Смена тайтла перестраивает список сразу, без переоткрытия диалога;
- **значения без привязки видны всегда** — иначе их нельзя было бы
  ни выбрать, ни привязать;
- **новые значения привязываются сами**: персонаж, введённый руками
  при выбранном тайтле, после отправки попадает в словарь уже внутри
  него (`record_usage` обрабатывает поля от родителей к зависимым).
  Привязка выполняется, когда у родителя выбрано ровно одно значение;
- **тёзки — разные записи**: «Lara» в «TombRider» и «Lara» в другом
  тайтле живут отдельно (дубли считаются в пределах родителя);
- **значение без родителя усыновляется** при первом использовании
  вместе с тайтлом — словарь связывается по мере работы;
- **удаление тайтла удаляет его персонажей** (каскад в схеме, решение
  от 15.08.2026) — редактор словаря предупреждает об этом;
- **удаление всего родительского поля** словарь зависимого не трогает:
  значения отвязываются (как при смене связи), поле становится
  независимым с целым словарём (аудит 2026-08-15); удаление поля
  и шаблона в интерфейсе — с подтверждением.

Связи правятся руками в редакторе «Словарь…»: у каждого значения —
выбор родителя, у строки добавления — тайтл для новых значений.

## Переименование файла при отправке

У шаблона подписи есть необязательный **шаблон имени файла**
(`filename_pattern`), например `{Author}, {video} ({Genre}) {quality}
(@{channel})`. Плейсхолдеры: `{video}` — название видео (нарочно
не `{title}`: у каналов бывает поле «Title», различие только регистром
путало; миграция `b6d4f82e9c13`); `{ИмяПоля}` — значения
поля через запятую (без решёток); `{quality}` — меньшая сторона кадра
видео (ffprobe); `{channel}` — @имя канала; неизвестные остаются как есть.

После сборки подписи движок вычисляет имя (`render_filename`: подстановка,
очистка недопустимых символов, расширение исходника), на странице
появляется строка «Переименовать при отправке» — имя правится руками,
галочка отключает. Предел длины имени — **в байтах UTF-8**
(`MAX_FILENAME_BYTES` = 240, с учётом расширения): лимит файловых
систем байтовый (ext4 — 255 байт), кириллица занимает два байта
на букву; обрезка не рвёт символ посередине. Поверх действует лимит
Telegram (`TELEGRAM_MAX_STEM_CHARS` = 78 символов стема — длиннее
сервер молча режет и чистит, см. [правила](telegram-filenames.md)):
стем режется так, чтобы имя заканчивалось **законченным словом** —
срез в середине слова откатывается к концу предыдущего, висячие знаки
препинания убираются; сплошное слово без разделителей режется как есть. При отправке `PostsService` переименовывает файл
**и его кадр-превью** на диске (`PostDraft.rename_to`), занятое имя
или путь в имени — понятная ошибка до отправки.

## Разбор имени файла в название (пакетная публикация)

Элементарный детерминированный разбор — до будущего смыслового разбора
силами ИИ (тот появится на этапе генерации и эти правила не отменит).

Модель одна: **разбор — цепочка замен по регулярным выражениям**. Шаг
(`TitleStep`) — это пара «что найти» → «на что заменить»; каждый
следующий работает по результату предыдущего. **Пустая замена означает
удаление**, поэтому отдельного правила «удалить» не нужно: удаление —
частный случай замены. Пробел в замене — обычное значение, а не
«ничего»: им разбивают слипшиеся слова (`[_-]` → пробел — самый ходовой
шаг, с него обычно и начинают цепочку). В замене допустимы ссылки
на группы выражения (`\1`, `\g<имя>`) — найденное можно не выбрасывать,
а переписать.

Единственное, что заменой не выражается, — **регистр**: он остался
отдельным правилом поверх цепочки («Каждое Слово» поднимает только
первую букву слова, не ломая «iPhone»; «Только первая буква» — по фразе).
В конце пробелы схлопываются. Пустой результат откатывается к исходному
названию: пост без названия хуже поста с сырым. Битый шаг пропускается
со следом в логе — разбор сотни имён не падает из-за опечатки, а причину
показывает форма (`compile_step` — общая точка проверки; проверяет обе
части шага, потому что шаблон замены тоже бывает битым: ссылка
на несуществующую группу).

Чистая функция `parse_title` и правила `TitleParseRules`
(`steps` + `case`) живут в сервисе подписей. Готовые выражения —
`TITLE_STEP_PRESETS` (единая точка: подписи пунктов и шаблоны не должны
жить в двух местах): скобки с содержимым, даты, слова из одних цифр,
номера по краям, перечень слов, метки качества и релиза. Разделителей
(`_`, `-`) среди заготовок нет намеренно: это замена на пробел, а не
удаление, и пишется она парой «`[_-]` → пробел» без всякой заготовки.

**Заготовка дат** покрывает ходовые написания: `2024-01-31`,
`31.01.2024`, `31.01.24`, `24-01-31`, слитное `20240131`. Разделитель
внутри одной даты одинаковый (`31.01-24` датой не считается), по краям
запрещена цифра — куски длинных чисел не выкусываются. **Одиночный год
датой не считается**: «Blade Runner 2049» переживает разбор, а кому
мешает и год — берёт заготовку «слова из одних цифр».

В окне пакетной публикации — разворачивающийся блок «Правила разбора
имени файла»: поле «что найти», поле «на что заменить» (пусто —
удаление), выпадающий помощник (вставляет заготовку в поле выражения —
дальше её правят руками) и кнопка «Применить». Применённый шаг встаёт
в конец цепочки, названия всех строк пересчитываются **от исходного
имени файла через всю цепочку** — поэтому убранный из середины шаг
отменяется честно, а не «как получилось» (шаг снимается **по номеру**:
два одинаковых шага в цепочке иначе снимали бы друг друга). В списке
применённых шагов замена показана в кавычках — «` `» против «удалить»:
без кавычек пробел в замене был бы невидим.

Цепочка сохраняется настройкой канала `title_parse_rules` (ADR-0013)
и наполняет блок при следующем пакете; сама по себе она не применяется.
Ручные правки подписей при пересчёте перезаписываются: правила
настраивают до точечной правки; без общего шаблона подписи результат —
жирное название само по себе.

Токены настройки: `sub:<JSON-пара>` — шаг целиком (`["выражение",
"замена"]`; JSON, потому что и выражение, и замена бывают любыми,
и разделитель-символ рано или поздно встретился бы внутри них самих)
и `case:<режим>`. Незнакомые токены игнорируются ради прямой
совместимости — настройка, записанная более новой версией, не ломает
старую. Поддержки двух прежних поколений формата (галочки `brackets`,
`separators`, `remove:…` и шаги `step:<выражение>` без поля замены)
больше нет: приложение не выходило за пределы машины автора, и в рабочей
базе таких настроек не оказалось ни одной (проверено 2026-09-12).

## Связанные документы

- [Модель данных](../05-data/data-model.md) · [ADR-0011](../02-architecture/decisions/0011-publish-via-userbot.md)
- [Правила Telegram для имён файлов](telegram-filenames.md)
